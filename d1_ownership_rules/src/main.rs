fn main() {
    println!("OWNERSHIP RULES");
    println!("\n1. Each value in Rust has a variable that's called it's owner.");
    println!("2. There can only be one owner for each variable at any one time.");
    println!("3. When the owner goes out of scope the value is dropped.");
}
